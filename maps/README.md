这里放底图图片。

- 默认底图是 `maps/卡洛西亚大陆/big_map_17173.png`。
- 用户可以把任意 `.png`、`.jpg`、`.jpeg`、`.webp`、`.bmp` 图片放到 `maps/` 或其子目录，并在设置里选择。
- 路线和标注会按文件里的原始 `x/y` 像素坐标绘制。
- 自定义底图可以正常使用，但如果它和当前路线/标注数据不是同一套像素坐标，点位可能会偏移。
